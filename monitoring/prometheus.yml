global:
  scrape_interval: 10s       # kriteria 5: collecting metrics period 10 detik

scrape_configs:
  - job_name: "jenkins"
    metrics_path: /prom      # kriteria 5: metrics path /prom
    static_configs:
      - targets: ["jenkins:8080"]
